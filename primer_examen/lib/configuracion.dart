import 'package:flutter/material.dart';

class ConfiguracionMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Configuracion del sistema",
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
